import { FollowUserCommandHandler } from './follow-user.command';
import { UnfollowUserCommandHandler } from './unfollow-user.command';

export const CommandHandlers = [
  FollowUserCommandHandler,
  UnfollowUserCommandHandler,
];

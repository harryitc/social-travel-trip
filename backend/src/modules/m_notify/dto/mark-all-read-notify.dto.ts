import { ApiProperty } from '@nestjs/swagger';

/**
 * DTO for marking all notifications as read
 */
export class MarkAllReadNotifyDto {
  // No properties needed as we'll use the user ID from the request
}

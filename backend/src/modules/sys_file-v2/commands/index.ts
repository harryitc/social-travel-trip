import { FileUploadCommandHandler } from './file-upload.command';
import { FileDeleteCommandHandler } from './file-delete.command';

export const CommandHandlers = [
  FileUploadCommandHandler,
  FileDeleteCommandHandler,
];

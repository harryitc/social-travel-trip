/**
 * Trip detail page - Uses parallel routes for 3-column layout
 * The URL parameter [id] is automatically available to parallel routes
 * The actual content is rendered by:
 * - @groups/page.tsx (left column) - will auto-select group based on URL
 * - @chat/page.tsx (center column) - will show chat for selected group
 * - @details/page.tsx (right column) - will show details for selected group
 */
export default function TripPage() {
  return null;
}